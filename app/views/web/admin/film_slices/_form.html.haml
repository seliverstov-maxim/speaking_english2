= simple_form_for [:admin, @form], html: { class: 'form-horizontal' } do |f|
  = f.error_notification
  = f.input :title
  = f.input :description
  = f.input :start_at, as: :integer
  = f.input :stop_at, as: :integer

  = f.input :video_file_ids, as: :select, collection: video_collection, input_html: { multiple: true, class: 'select2' }
  = f.input :sub_file_ids, as: :select, collection: sub_collection, input_html: { multiple: true, class: 'select2' }
  = f.input :tag_ids, as: :select, collection: tag_collection, input_html: { multiple: true, class: 'select2' }

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to t('helpers.links.cancel'), admin_film_slices_path, :class => 'btn'
