= simple_form_for [:admin, @form], html: { class: 'form-horizontal' } do |f|
  = f.error_notification
  = f.input :url

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to t('helpers.links.cancel'), admin_video_files_path, :class => 'btn'
