= simple_form_for [:admin, @form], html: { class: 'form-horizontal' } do |f|
  = f.error_notification
  = f.input :title
  = f.input :description

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to t('helpers.links.cancel'), admin_film_slices_path, :class => 'btn'
